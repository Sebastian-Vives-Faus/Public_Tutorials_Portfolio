import { createTheme } from "@mui/material";

export const theme = createTheme({
  status: {
    danger: "#e53e3e",
  },
  palette: {
      type: 'dark',
    primary: {
      light: "#757ce8",
      main: "#bd49d1",
      dark: "#002884",
      contrastText: "#fff",
    },
    
  },
});
